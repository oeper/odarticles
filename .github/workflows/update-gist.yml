name: Deploy Article Hub
on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout your repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Inject GitHub Token (temporary)
      - name: Inject GitHub Token
        run: |
          cp index.html index.html.bak
          sed -i "s|window.GITHUB_TOKEN || \"\"|window.GITHUB_TOKEN || '${{ secrets.DASASDASD }}'|" index.html

      # 3️⃣ Deploy to GitHub Pages
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./

      # 4️⃣ Restore original file so token never gets committed
      - name: Restore index.html
        run: mv index.html.bak index.html
